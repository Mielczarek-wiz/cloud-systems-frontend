/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
/*
async redirects() {
    return [
      {
        source: "/0",
        destination: "/",
        permanent: true,
      },
    ];
  },
*/
